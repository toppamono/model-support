# AGENTS.md

## Project purpose
このプロジェクトは、小学校の特別支援教室・自立活動で、教師が見当違いな指導・評価を選ぶことを減らすための意思決定支援ツールである。

## Source of truth
- GitHub の `main` ブランチを唯一の正式な最新版とする。
- 作業開始前に必ず `main` の最新状態を取得する。
- ローカルだけに残った変更を正式版として扱わない。
- ChatGPT/Codexのどちらが変更しても、最終的にGitHubへ反映する。
- 同じファイルを同時に変更しそうな場合は、Issueまたはブランチを分けて衝突を避ける。

## Non-negotiable principles
1. 児童を固定的な「タイプ」に分類しない。
2. 診断名・障害種から支援や教材を直接決定しない。
3. モデルは「現在の課題状態」を示す仮説として扱う。
4. 主モデル＋関連モデルを許容し、単一の正解を出さない。
5. 本人・学校・家庭の困り感を分けて記録する。
6. 周囲だけが困っている場合、本人を変える支援だけでなく環境調整を検討する。
7. 出力は診断ではない。観察、仮説、試行、再評価につなげる。
8. 実在児童の個人情報をリポジトリへ保存しない。
9. 公開サンプルは必ず架空ケースを使う。
10. UI文言は教師が学校現場で短時間に扱える日本語を優先する。

## Development rules
- 既存の基本原則を変更する場合は、README/docsも同時に更新する。
- 支援モデルを追加・変更する場合は `data/models.json` を単一の基準データとして扱う。
- ローカル保存を基本とし、外部送信機能を追加する場合は明示的な設計レビューを行う。
- 自動推薦は理由を表示し、教師が修正できる設計にする。
- 「ADHDだから」「ASDだから」のような診断名直結ロジックを実装しない。
- 重要な仕様変更はGitHub Issue/PRに判断理由を残す。
- 大きな変更は `main` に直接上書きせず、ブランチまたはPRを使う。
